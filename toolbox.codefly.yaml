name: grpc
version: 0.0.10
description: |
  gRPC reflection-based service/method introspection as typed RPCs.
  Canonical owner of the `grpcurl` binary — the bash toolbox refuses
  every `grpcurl` invocation and routes here.

  list_services, describe_service, and describe_method are read-only
  reflection operations. grpc.call resolves protobuf descriptors at
  runtime and invokes unary RPCs; it is marked potentially destructive.

agent:
  kind: codefly:toolbox
  name: grpc
  publisher: codefly.dev
  version: 0.0.10

sandbox:
  read_paths: []
  write_paths: []
  # Network: outbound TCP to the dial target. Each call opens a
  # short-lived connection and tears it down. Constraining to
  # specific hosts is the host's job (OS sandbox).
  network: open
  unix_sockets: []

canonical_for:
  - grpcurl

permissions:
  required:
    - action: grpc.list_services
      reason: Discover services exposed by a reflection-enabled endpoint.
    - action: grpc.describe_service
      reason: Read one reflected service descriptor.
    - action: grpc.describe_method
      reason: Read one reflected method descriptor.
  optional:
    - action: grpc.call
      reason: Invoke an explicitly selected unary RPC when host policy permits it.
  risk_levels:
    grpc.call: high

spec: {}
